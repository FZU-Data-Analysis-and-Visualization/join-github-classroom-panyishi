# AI 使用说明

- **课程名称**：数据分析与可视化
- **学生姓名**：潘逸诗
- **学号**：032402125
- **考试时间/地点**：2026年06月24日 14:00-17:30 / 旗山东2-102
- **分析数据集**：`ershoufang_list.csv`（福州二手房房源数据分析）

---

### 1. 使用的 AI 工具
在本次期末上机数据分析任务中，本人主要使用了大语言模型 **Gemini** 作为核心辅助工具。

### 2. 使用 AI 的具体环节
 1. **数据清洗与读取代码优化：**
   针对原始 CSV 数据读取可能遇到的编码报错问题，参考 AI 提示编写了 `try-except` 捕获异常的代码，实现 `utf-8-sig` 与标准 `utf-8` 的自动切换。
 3. **隐性缺失值处理：**
   由于原始数据中 `总价`、`均价`、`面积` 被单位文本（万、元/㎡、㎡）包裹，普通的 `isnull()` 无法直接找出缺失值。AI 辅助提供了利用 `pd.to_numeric` 将非法文本强制转为 `NaN` 的清洗思路。
 5. **透视表特殊排序：**
   在使用 `pd.pivot_table` 生成 `[市区 × 房龄]` 二维透视表时，由于房龄“2年内”、“2-5年”等属于字符串，默认排序会发生错乱。AI 辅助提供了通过自定义列表强行按时间先后顺序排列的代码框架。
1. **图表绘制参数：** 
   辅助编写了带有核密度估计（KDE）的直方图代码，并提示了配置 matplotlib 跨平台中文字体（如 'SimHei'）防乱码的常规设置。

---
### 3. 主要提问内容或提示词摘要
* **问题 1（数据清洗）：** 
* “我有一份二手房挂牌数据。部分数值列因为带有‘万’、‘元/㎡’等单位被识别成了字符串，而且里面可能包含普通 isnull() 找不到的隐性空值。请帮我写一段 Python 清洗代码，把单位去掉并转为 float64，同时帮我统计各列真正的缺失值情况。”
* **问题 2（透视表排序）：** 
* “在 Python 中用 `pd.pivot_table` 做出一个行是‘市区’、列是‘房龄’的挂牌单价中位数矩阵。因为房龄是‘2年内’、‘2-5年’这样的字符串，默认排序不对，怎么让列按照房龄从小到大排列？”
* **问题 3（图表绘制）：** 
* “我想用 matplotlib 和 seaborn 画一个房价的全局分布直方图。要求加上核密度曲线，用不同颜色虚线画出平均值和中位数的位置，并解决中文字体乱码、负号显示不出来的问题。”

### 4. 我对 AI 输出做了哪些检查、修改或取舍
**本报告中所有核心数据及最终的统计学判断，均由本人进行了严格的二次核对与修正，拒绝盲从 AI 的输出：**

* **数据真实性核对：** 
AI 在润色摘要和文字报告时，自动生成的数字均为虚构。我运行完代码后，严格核对并将其替换成了后台真实跑出来的统计结果：如确认原始数据集规模为 `11856行 × 18列`；查出并剔除了 26条 隐性缺失数据，剩余有效样本 11830条。
* **地区标签校准：** 
检查并删除了 AI 在文字叙述中盲目猜测生成的非本地行政区划，严格限制并对齐为福州本地真实的行政市区（鼓楼、晋安、台江、仓山、闽侯、马尾等）。
* **统计学逻辑修正（核心取舍）：**
 AI 曾建议直接利用 IQR（四分位距）法切除所有高于统计学上限的价格异常值。我经过人工审计，驳回了 AI 的删除建议。因为在二手房市场中，高总价的别墅或豪宅属于真实的偏态长尾分布，并非录入错误。盲目切除会丢失高端市场特征，因此我选择在清洗阶段对其予以保留，但在后续分析涉及均值计算时，主动参考抗噪能力更强的“中位数”指标。

### 5. 哪些分析结论是我根据数据自行判断得到的
以下三项核心分析结论，均为本人根据 Python 运行出来的描述统计、空间聚合及相关性矩阵结果，独立观察并推导得出：

1.  **各行政市区的价格与供给分化特征：**
    通过地理维度的多指标聚合，我发现福州二手房市场呈现出明显的空间分化。晋安区（4346套）与仓山区（3926套）是挂牌量最大的供给大盘，代表了市场的主要刚需流转地；但在价格层面，鼓楼区凭借核心的资源优势（学区、行政、商业等）垄断了价值高地，其挂牌单价中位数逼近 27,243 元/㎡，远超其他外围和远郊板块。
2.  **建筑面积是决定房产总价的核心硬件变量：**
    根据 Pearson 相关性定量矩阵，建筑面积（`area_cleaned`）与总挂牌价（`total_price_cleaned`）的正相关系数高达 0.672。这表明在福州市场中，尽管单价容易受到地铁、学区等外部因素的扰动，但房屋自身的物理体量（面积）依然是决定房产总价值最关键的基准变量。
3.  **房龄折旧效应对购房者户型选择的倒逼：**
    结合 `[市区 × 房龄]` 的二维透视表，可以看出除了个别次新房外，绝大多数城区的房产单价都随着房龄的增长而出现明显的折旧下滑（如鼓楼区单价从中位数的 32,925 元/㎡ 随房龄降至 21,834 元/㎡）。这种由于设备和物业老化带来的折旧风险，在实际买房场景中，正倒逼改善型刚需客群在空间尺度上不再盲目追求过大面积，而是全面向 90–140㎡ 这种高坪效、标准化的三居或四居改善型户型集中。