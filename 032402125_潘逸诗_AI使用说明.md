# AI 使用说明

- **课程名称**：数据分析与可视化
- **学生姓名**：潘逸诗
- **学号**：032402125
- **考试时间/地点**：2026年06月24日 14:00-17:30 / 旗山东2-102
- **分析数据集**：`ershoufang_list.csv`（福州二手房房源数据分析）

---

### 1. 使用的 AI 工具
在本次期末上机数据分析任务中，本人主要使用了大语言模型 **Gemini** 作为核心辅助工具。

### 2. 使用 AI 的具体环节
在整个项目的生命周期中，本人并未盲目依赖 AI 自动生成报告，而是将其精准定位为“技术文档检索支架”与“代码工程化健壮性优化器”。具体应用的底层技术环节包括：
- **数据质量隐性特征探测**：针对夹杂行业单位、导致数据类型劣化的字符型字段，查询如何设计前置、非破坏性的脱敏代码，以攻克欺骗 `isnull()` 算子的隐性缺失值识别痛点。
- **高级统计透视表列重排**：针对 `pd.pivot_table` 算子自动生成的无序文本列（房龄），检索如何在不污染、不重写原始数据的前提下，实现原位的时间逻辑序列重排。
- **图表规范化与视觉降噪**：检索 `matplotlib` 与 `seaborn` 的多坐标轴（双 Y 轴）协同排版方案，解决两套独立坐标系的网格线交织冲突，并优化学术级高分辨率 `dpi` 配置与视觉降噪。
- **专业表达润色与文本精简**：优化报告摘要、图表数理研判中偏向口语化、直觉化的文字表述，使其转化为符合专业统计学规范和行业商业洞察的实证叙述。

### 3. 主要提问内容或提示词摘要
为了引导 AI 给出高质量、无报错的底层技术支架，本人摒弃了模糊的口语化提问，而是面向具体的数据陷阱与工程痛点，设计了具有清晰数理定义的提示词：
- **提示词摘要 1（探测隐性缺失）**：“在 pandas 中，有几个包含单位的字符串列（如 '199万'），直接用 `.isnull()` 检查显示没有缺失值，但在去掉单位强转 float 之后，切除了 26 条缺失值。如何能在不破坏原 DataFrame 结构的前置审计阶段，精准捕捉并打印出这 26 条隐性缺失值，使得质量看板符合严格的数理闭环？”
- **提示词摘要 2（调整数据透视表顺序）**：“我生成了一个数据透视表，列名是房屋的房龄类别（2年内、5年内、10年内、10年以上）。现在 pandas 自动生成的列顺序是乱的。怎么在不修改原始数据的情况下，强行让透视表的列按照正常的历史时间演进顺序横向排列？”
- **提示词摘要 3（优化双轴图排版与网格）**：“我想用 seaborn/matplotlib 画一个双轴图，左边轴是柱状图表示各区挂牌量，右边轴是折线图表示均价中位数。怎么正确设置共享 X 轴的双 Y 轴？并且如何关闭其中一个轴的网格线，防止两套网格线交织导致画面杂乱？”

### 4. 我对 AI 输出做了哪些检查、修改或取舍
大语言模型具备极强的泛化编写能力，但往往缺乏针对具体垂直行业的业务洞察与实证严谨性。因此，本人在交互过程中始终保持批判性思维，对其输出进行了严格的代码审计与逻辑矫正：
- **力排粗暴的异常值切除建议**：在模块 5 处理异常值时，AI 基于通用算法机械地建议利用四分位距（IQR）边界直接切除所有超出上限的高总价房源。本人在独立审查时，考虑到房地产市场天然具有右偏态的长尾分布特征，这些高端总价资产属于真实的城市核心区优质偏态“极值”而非“输入错误”。盲目切除会导致高端豪宅市场的结构特征完全丢失。因此，本人果断摒弃了 AI 的通用建议，选择在清洗阶段**予以保留**，并通过在后续统计中引入抗噪能力更强的“中位数”算子来平抑方差，实现了业务背景与统计学方法的完美结合。
- **全域硬编码数据的校准与去伪**：AI 在协助润色文字表达时，极易陷入模型“幻觉”，凭空编造无数据支撑的趋势或错误的数字。本人在最终定稿前，将 AI 润色的所有文本段落与前文 Python 代码实际运行 `ershoufang_list.csv` 产生的真实数理指标进行了逐一核对与手工修正。例如，将空泛的“高度相关”改写为真实的 Pearson 相关系数 `0.672`，将模糊的“价格很高”对齐为真实的鼓楼单价中位数 `27,243 元/㎡`，确保了整篇报告达到无瑕疵的实证严谨度。

### 5. 哪些分析结论是我根据数据自行判断得到的
整篇分析报告中的核心业务洞察、时空非均衡分化研判、以及置业防御策略，均是本人基于代码实际运行结果，独立开展的自主数理判断与深度逻辑推导：
- **空间供需非对称极化特征的独立判定**：根据模块 6.3 的聚合数据与图表 2 的双轴走势，本人敏锐地发现晋安与仓山两区垄断了市场超 50% 的供给大盘（均突显出 3,100 套以上的挂牌体量），而鼓楼区虽供给量非榜首，其资产均价中位数却一骑绝尘。由此独立推导出“福州存量房市场是由庞大的中低价刚需盘筑底、核心优质资源强力驱动价格极化”这一空间空间分化结论。
- **资产总价值第一驱动锚点的量化确立**：基于相关性热力图（图表 4）中输出的 Pearson 线性相关矩阵，本人独立捕获到挂牌总价与建筑面积的相关系数（`0.672`）显著高于其他维度，从而绕开 AI 泛泛的区位决定论，定量确立了“无论外部非结构化配套如何波动，物理建筑面积依然是决定房产总清算价值的第一核心底座”这一核心数理判定。
- **时间折旧效应与户型标准化收敛的归纳**：结合模块 6.4 的市区房龄交互矩阵与图表 3 的并列分位数箱线图，本人自主观察到随着房龄步入 10 年以上，各行政区资产面临不可逆的单价水位大滑坡，而 2 年内与 5 年内次新房的建筑面积离散箱体则大幅收窄、中位数稳步上移。由此独立归纳出“存量房市场时间折旧成本高额贴现，且近年来开发商正加速向 90-140㎡ 高坪效标准化改善户型集中收敛”的历史演变规律。